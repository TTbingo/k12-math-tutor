---
title: "（填写文件标题）"
version: 1.9.0
last_updated: 2026-07-21
usage: "按需查阅，不自动展开"
---

# 文件用途

（说明本文件在 k12-math-tutor 中的职责、被谁引用、何时查阅）

---

> **Frontmatter 规范**：`references/` 下所有 `.md` 文件必须带本 frontmatter，字段为 `title` / `version` / `last_updated` / `usage`。
> - `version` 须与 `SKILL.md` 版本号保持一致（`validate-structure.py` 会校验）。
> - `title` 为该文件的简短可读标题；`usage` 说明查阅方式。
> - 新增文件请**基于本模板**创建；模板文件本身（`_TEMPLATE.md`）不会被纳入校验与引用表。
